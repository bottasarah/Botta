from guizero import App, Text, Textbox

app = App(title='Interfaccia')

hello_text = Text(app, text='GRAFICO', font='Helvetica', size=20)

whatever = TextBox(app, width=50, multiline=True, height=2)
whatever.value='Dati'

push = PushButton(app, text='premi')
push.width=10
push.height=5





app.display()
